[project]
name = "data-ai-pipeline"
version = "0.1.0"
description = "An orchestrated ETL pipeline for transforming archived job advertisements into structured relational data."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "pydantic>=2.13.4",
    "ruff>=0.15.12",
]
